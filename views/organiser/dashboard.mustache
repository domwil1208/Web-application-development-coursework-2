<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Organiser Dashboard</title>
  <link href="/styles.css" rel="stylesheet">
  <link href="/icons.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
</head>
<body>
  <div class="container py-5">
    <h1 class="mb-4">Organiser Dashboard</h1>

    <a href="/logout" class="btn btn-danger mb-3">Logout</a>

    <h2>Courses</h2>
    <a href="/organiser/add-course" class="btn btn-primary mb-3">+ Add Course</a>

    {{#courses}}
      <hr>
      <h3>{{name}} ({{duration}}) - £{{price}} @ {{location}}</h3>
      <p>{{description}}</p>
      <a href="/organiser/add-class?courseId={{_id}}" class="btn btn-success mb-2">+ Add Class</a> |
      <a href="/organiser/delete-course/{{_id}}" class="btn btn-danger mb-2" onclick="return confirm('Delete this course and its classes?')">Delete Course</a>

      {{#classes.length}}
        <h4>Classes</h4>
        <ul class="list-group">
          {{#classes}}
            <li class="list-group-item d-flex justify-content-between align-items-center">
              <div>
                <strong>{{datetime}}</strong> - {{description}} @ {{location}}
              </div>
              <div>
                <a href="/organiser/class-participants/{{_id}}" class="btn btn-warning btn-sm">View Class Participants</a>
                <a href="/organiser/edit-class/{{_id}}" class="btn btn-secondary btn-sm">Edit</a> |
                <a href="/organiser/delete-class/{{_id}}" class="btn btn-danger btn-sm" onclick="return confirm('Delete this class?')">Delete</a>
              </div>
            </li>
          {{/classes}}
        </ul>
      {{/classes.length}}

      {{^classes.length}}
        <p><em>No classes yet.</em></p>
      {{/classes.length}}

    {{/courses}}

    <a href="/organiser/organisers" class="btn btn-info mt-4">Manage Organisers</a>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
